#!/bin/bash
if [ -f $HOME/.tgbot ]; then
. $HOME/.tgbot
else
echo -e "TG_BOTAPI=\"\"\nTG_JSONS=\"\"" > $HOME/.tgbot
fi

WOD=$PWD
cd $TG_JSONS
if [ "$1" ] && [ "$2" ]; then
curl -s -X POST -H "Content-Type: application/json" -d @$2.json $TG_BOTAPI/$1
fi
cd $WOD
